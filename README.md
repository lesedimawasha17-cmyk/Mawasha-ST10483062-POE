# Mawasha-ST10483062-POE
This is my Web Development Project folder

. in the lecture’s feedback for part 1there were a few mistakes that were highlighted that needed to be fixed before I could continue with my part 2

. I created folders for both CSS and java script because in part 1 I had just created files.
 
. I created a folder for images and put the images I was working with inside the folder.

. my gallery.html file previously hat nothing but now I put the code.

. because the navigation link only existed in the homepage, I added it to every page so users can get to any part of the site from everywhere.

. my pages had used <h1>, <p>, <footer> with no structural wrapping so I added <header> around the site title, <nav> around the navigation links, <main> around each page’s unique content, id attributes on each so they are individually identifiable.

. contact page: cut off email address, broken and I fixed that.

. fixed the space that was on the nav link in the file name contact -us.html to contact us.html.

. added the missing <meta charset> on all the pages.

. I also added a comment above every major section on every page explaining what it does.

. the CSS has inline and external code

. I also updated my read me file which had no information.

